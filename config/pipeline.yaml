parallelism:
  cycles: sequential
  file_types: parallel

styles:
  current:
    file_types: [indiv, cn, cm, oth, oppexp, ccl, weball, pas2, indexp]
    change_detection: true
  historical:
    file_types: [indiv, cn, cm, oth, oppexp, ccl, weball, pas2, indexp]
    change_detection: false

cycles:
  - cycle: 2024
    style: current
  - cycles: "2010-2022"
    style: historical
